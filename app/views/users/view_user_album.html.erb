
<div class="body-edit">
    <div>
        <% if @user.avatar_attachment %>
            <%= image_tag(@user.avatar, alt: "User Avatar", class:" rounded-circle") %>
        <% else %>
            <%= image_tag('user.jpg', alt: "User Avatar", class: " rounded-circle", style: 'height:200px;width:200px') %>
        <% end %>
    </div>
    <div>
        <button>
            following
        </button>
        <h2 style="font-size: 40px"> <%= @user.firstname %> <%= @user.lastname %></h2>
        <div class="body-edit-item">
            
            <p> <%= link_to:"view_user", class: "mx-1 text-decoration-none fs-4", style: "color: black;" do %>
                <span ><%= @photos.size %> </span>PHOTOS
            <% end %>
            </p>
            <p> <%= link_to:"view_user_album", class: "mx-1 text-decoration-none fs-4", style: "color: blue;" do %>
                <span ><%= @albums.size %> </span>ALBUM
            <% end %>
            </p>
            <p> <%= link_to:"root", class: "mx-1 text-decoration-none fs-4", style: "color: black;" do %>
                <span ><%= @followings.size %> </span>FOLLOWINGS
            <% end %>
            </p>
            <p> <%= link_to:"view_user_follower", class: "mx-1 text-decoration-none fs-4", style: "color: black;" do %>
                <span ><%= @followers.size %> </span>FOLLOWERS
            <% end %>
            </p>

        </div>
    </div>
</div>
<div  class="row">
    <% @albums.each do |album| %>
        <div class="col-3">
            <div class="", style="margin: 10px; box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3); padding:5px; height: 250px " >
                <div class="profile-album-card ">
                    <% album.photos.each_with_index do |photo, index| %>
                        <div class= "album-photo-image">
                            <%= image_tag(photo.image.url, alt: "Photo Picture", class: "img-thumbnail rotate-image-#{index}" )%>
                        </div>
                    <% end %>
                   
                </div>
             
            </div> 
            <div style="font-size: 20px; text-align: center">
                <p class="card-text"><%= album.name %></p> 
            </div> 
        </div>
    <% end %> 
    <% @albums.each do |album| %>
        <div class="col-3">
            <div class="", style="margin: 10px; box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3); padding:5px; height: 250px " >
                <div class="profile-album-card ">
                    <% album.photos.each_with_index do |photo, index| %>
                        <div class= "album-photo-image">
                            <%= image_tag(photo.image.url, alt: "Photo Picture", class: "img-thumbnail rotate-image-#{index}" )%>
                        </div>
                    <% end %>
                   
                </div>
             
            </div> 
            <div style="font-size: 20px; text-align: center">
                <p class="card-text"><%= album.name %></p> 
            </div> 
        </div>
    <% end %> 
    <% @albums.each do |album| %>
        <div class="col-3">
            <div class="", style="margin: 10px; box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3); padding:5px; height: 250px " >
                <div class="profile-album-card ">
                    <% album.photos.each_with_index do |photo, index| %>
                        <div class= "album-photo-image">
                            <%= image_tag(photo.image.url, alt: "Photo Picture", class: "img-thumbnail rotate-image-#{index}" )%>
                        </div>
                    <% end %>
                   
                </div>
             
            </div> 
            <div style="font-size: 20px; text-align: center">
                <p class="card-text"><%= album.name %></p> 
            </div> 
        </div>
    <% end %> 
    
              
</div>
